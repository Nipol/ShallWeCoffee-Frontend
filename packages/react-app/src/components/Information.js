import React from "react";


var targetUser  = window.location.pathname.split('/');
var targetManagerAddr = await provider.resolveName(targetUser);

var name = ;
var company = ;
var location = ;
var contactArr = ;

function getName() { return (name); }
function getCompany() { return (company); }
function getLoc() { return (location); }

function Information() {
    return(
            <div></div>
    );
}